#include <stdio.h>

// Typecasting Syntax
// (Type) value;

int main()
{
    int a = 4;
    float b = (float)54/5;                      // You can also use double instead of float
    printf("The Value of a is : %f", b);
    
    return 0;
}